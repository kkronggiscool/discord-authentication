<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Authentication</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2c2f33;
            color: #ffffff;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }
        h1, h2 {
            color: #7289da;
        }
        img {
            border-radius: 50%;
            margin: 10px 0;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            background-color: #23272a;
            margin: 5px 0;
            padding: 10px;
            border-radius: 5px;
            display: flex;
            align-items: center;
        }
        li img {
            border-radius: 5px;
            margin-right: 10px;
        }
        button {
            background-color: #7289da;
            color: #ffffff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        button:hover {
            background-color: #5b6eae;
        }
        a {
            text-decoration: none;
        }
        .container {
            text-align: center;
            max-width: 600px;
            margin: 0 auto;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the App</h1>
        <% if (user) { %>
            <p>Hello, <strong><%= user.username %></strong>!</p>
            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar" width="100">
            <h2>Your Managed Servers:</h2>
            <ul>
                <% if (user.guilds && user.guilds.length > 0) { %>
                    <% user.guilds.forEach(guild => { %>
                        <li>
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="Guild Icon" width="50">
                            <span><%= guild.name %></span>
                        </li>
                    <% }); %>
                <% } else { %>
                    <li>No managed servers available</li>
                <% } %>
            </ul>
            <a href="/logout"><button>Logout</button></a>
        <% } else { %>
            <a href="/auth/discord">
                <button>Login with Discord</button>
            </a>
        <% } %>
    </div>
</body>
</html>